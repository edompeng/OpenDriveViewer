name: Cross-Platform Build & Release Pipeline

on:
  push:
    branches: [ "main" ]
    tags:
      - 'v*'
  pull_request:
    branches: [ "main" ]

permissions:
  contents: write

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            qt_host: 'linux'
            qt_arch: 'linux_gcc_64'
            vcpkg_triplet: 'x64-linux'
            qt_version: '6.9.*'
            artifact_name: 'app-linux.tar.gz'
            
          - os: macos-14
            qt_host: 'mac'
            qt_arch: 'clang_64'
            vcpkg_triplet: 'arm64-osx'
            qt_version: '6.9.1'
            artifact_name: 'app-macos.dmg'
            
          - os: windows-2022
            qt_host: 'windows'
            qt_arch: 'win64_msvc2022_64'
            vcpkg_triplet: 'x64-windows'
            qt_version: '6.9.1'
            artifact_name: 'app-windows.zip'

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install System Dependencies (Linux)
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1-mesa-dev libglu1-mesa-dev xvfb patchelf libproj-dev

      - name: Install System Dependencies (macOS)
        if: matrix.os == 'macos-14'
        run: |
          brew install proj

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ matrix.qt_version }}
          host: ${{ matrix.qt_host }}
          target: 'desktop'
          arch: ${{ matrix.qt_arch }}
          cache: true
          cache-key-prefix: qt-${{ matrix.os }}

      - name: Cache vcpkg tool
        if: matrix.os == 'windows-2022'
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/vcpkg
          key: vcpkg-tool-${{ matrix.os }}-${{ hashFiles('vcpkg.json') }}

      - name: Cache vcpkg dependencies
        if: matrix.os == 'windows-2022'
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/vcpkg_installed
          key: vcpkg-${{ matrix.os }}-${{ matrix.vcpkg_triplet }}-${{ hashFiles('vcpkg.json') }}
          restore-keys: |
            vcpkg-${{ matrix.os }}-${{ matrix.vcpkg_triplet }}-

      - name: Setup vcpkg (Windows only for PROJ)
        if: matrix.os == 'windows-2022'
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgDirectory: '${{ github.workspace }}/vcpkg'
          vcpkgJsonGlob: '**/vcpkg.json'
          runVcpkgInstall: true
        env:
          VCPKG_DEFAULT_TRIPLET: ${{ matrix.vcpkg_triplet }}
          VCPKG_INSTALLED_DIR: '${{ github.workspace }}/vcpkg_installed'
          VCPKG_BINARY_SOURCES: 'clear;x-gha,readwrite'



      - name: Set Environment Variables
        shell: bash
        run: |
          echo "QT6_ROOT=${{ env.QT_ROOT_DIR }}" >> $GITHUB_ENV
          if [ "${{ matrix.os }}" = "ubuntu-22.04" ]; then
            echo "PROJ_ROOT=/usr" >> $GITHUB_ENV
          elif [ "${{ matrix.os }}" = "macos-14" ]; then
            echo "PROJ_ROOT=$(brew --prefix proj)" >> $GITHUB_ENV
          elif [ "${{ matrix.os }}" = "windows-2022" ]; then
            # Elegant solution: vcpkg in manifest mode puts artifacts in vcpkg_installed/<triplet>.
            # The packaging script will find all DLLs inside the 'bin' subdirectory.
            echo "PROJ_ROOT=${{ github.workspace }}/vcpkg_installed/${{ matrix.vcpkg_triplet }}" >> $GITHUB_ENV
          fi

      - name: Cache Bazel
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/bazel-repo
            ~/.cache/bazel-disk
          key: bazel-${{ matrix.os }}-${{ hashFiles('**/BUILD.bazel', '**/WORKSPACE*', 'MODULE.bazel', 'bazel/*.bzl', 'third_party/bazel/*.BUILD') }}
          restore-keys: |
            bazel-${{ matrix.os }}-

      - name: Build with Bazel
        shell: bash
        run: |
          BAZEL_OPTS="--repository_cache=$HOME/.cache/bazel-repo --disk_cache=$HOME/.cache/bazel-disk --keep_going"
          if [ "${{ matrix.os }}" = "ubuntu-22.04" ]; then
            bazel build -c opt --copt=-g --strip=never //... $BAZEL_OPTS --config=linux \
              --action_env=QT6_ROOT="${{ env.QT_ROOT_DIR }}" \
              --action_env=PROJ_ROOT="${{ env.PROJ_ROOT }}"
          elif [ "${{ matrix.os }}" = "windows-2022" ]; then
            # Fix path for Windows
            export BAZEL_OPTS="--repository_cache=$HOME/.cache/bazel-repo --disk_cache=$HOME/.cache/bazel-disk --keep_going"
            bazel build -c opt --copt=-Z7 --linkopt=-DEBUG //... $BAZEL_OPTS --config=windows \
              --action_env=QT6_ROOT="${{ env.QT_ROOT_DIR }}" \
              --action_env=PROJ_ROOT="${{ env.PROJ_ROOT }}" \
              --action_env=GITHUB_WORKSPACE="${{ github.workspace }}"
          else
            bazel build -c opt --copt=-g --strip=never //... $BAZEL_OPTS --config=macos \
              --action_env=QT6_ROOT="${{ env.QT_ROOT_DIR }}" \
              --action_env=PROJ_ROOT="${{ env.PROJ_ROOT }}"
          fi

      - name: Test with Bazel
        shell: bash
        run: |
          BAZEL_OPTS="--repository_cache=$HOME/.cache/bazel-repo --disk_cache=$HOME/.cache/bazel-disk --keep_going"
          if [ "${{ matrix.os }}" = "ubuntu-22.04" ]; then
            xvfb-run --auto-servernum bazel test -c opt //... --test_output=errors $BAZEL_OPTS --config=linux \
              --action_env=QT6_ROOT="${{ env.QT_ROOT_DIR }}" \
              --action_env=PROJ_ROOT="${{ env.PROJ_ROOT }}"
          elif [ "${{ matrix.os }}" = "windows-2022" ]; then
            bazel test -c opt //... --test_output=errors $BAZEL_OPTS --config=windows \
              --action_env=QT6_ROOT="${{ env.QT_ROOT_DIR }}" \
              --action_env=PROJ_ROOT="${{ env.PROJ_ROOT }}" \
              --action_env=GITHUB_WORKSPACE="${{ github.workspace }}" \
              --test_env=PATH
          else
            bazel test -c opt //... --test_output=errors $BAZEL_OPTS --config=macos \
              --action_env=QT6_ROOT="${{ env.QT_ROOT_DIR }}" \
              --action_env=PROJ_ROOT="${{ env.PROJ_ROOT }}"
          fi

      - name: Package (Linux)
        if: matrix.os == 'ubuntu-22.04' && startsWith(github.ref, 'refs/tags/v')
        run: |
          chmod +x scripts/deploy/package_linux.sh
          ./scripts/deploy/package_linux.sh
          # Create archive for release
          tar -czvf ${{ github.workspace }}/${{ matrix.artifact_name }} -C dist/linux OpenDriveViewer_linux_x64

      - name: Package (macOS)
        if: matrix.os == 'macos-14' && startsWith(github.ref, 'refs/tags/v')
        run: |
          chmod +x scripts/deploy/package_macos.sh
          ./scripts/deploy/package_macos.sh
          # The script produces dist/mac/OpenDriveViewer.dmg, move it to root for upload
          mv dist/mac/OpenDriveViewer.dmg ${{ github.workspace }}/${{ matrix.artifact_name }}

      - name: Package (Windows)
        if: matrix.os == 'windows-2022' && startsWith(github.ref, 'refs/tags/v')
        shell: pwsh
        env:
          QT6_ROOT: ${{ env.QT6_ROOT }}
          PROJ_ROOT: ${{ env.PROJ_ROOT }}
        run: |
          ./scripts/deploy/package_windows.ps1
          # The script produces a folder: dist/windows/OpenDriveViewer_win_x64
          Compress-Archive -Path dist/windows/OpenDriveViewer_win_x64 -DestinationPath ${{ github.workspace }}/${{ matrix.artifact_name }}

      - name: Upload Intermediate Artifact
        if: startsWith(github.ref, 'refs/tags/v')
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ github.workspace }}/${{ matrix.artifact_name }}
          retention-days: 1

      - name: Upload Symbols Artifact
        if: startsWith(github.ref, 'refs/tags/v')
        uses: actions/upload-artifact@v4
        with:
          name: symbols-${{ matrix.os }}
          path: |
            ${{ github.workspace }}/*symbols.zip
            ${{ github.workspace }}/*symbols.tar.gz
          retention-days: 1

  build-cmake:
    name: CMake Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            vcpkg_triplet: 'x64-linux'
            qt_version: '6.9.*'
          - os: macos-14
            vcpkg_triplet: 'arm64-osx'
            qt_version: '6.9.1'
          - os: windows-2022
            vcpkg_triplet: 'x64-windows'
            qt_version: '6.9.1'
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install System Dependencies
        if: matrix.os != 'windows-2022'
        run: |
          if [ "${{ matrix.os }}" = "ubuntu-22.04" ]; then
            sudo apt-get update && sudo apt-get install -y libgl1-mesa-dev libglu1-mesa-dev xvfb libproj-dev
          else
            brew install proj ninja
          fi

      - name: Cache vcpkg tool
        if: matrix.os == 'windows-2022'
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/vcpkg
          key: vcpkg-tool-${{ matrix.os }}-${{ hashFiles('vcpkg.json') }}

      - name: Cache vcpkg dependencies
        if: matrix.os == 'windows-2022'
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/vcpkg_installed
          key: vcpkg-cmake-${{ matrix.os }}-${{ matrix.vcpkg_triplet }}-${{ hashFiles('vcpkg.json') }}
          restore-keys: |
            vcpkg-cmake-${{ matrix.os }}-${{ matrix.vcpkg_triplet }}-

      - name: Setup vcpkg (Windows only for PROJ)
        if: matrix.os == 'windows-2022'
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgDirectory: '${{ github.workspace }}/vcpkg'
          vcpkgJsonGlob: '**/vcpkg.json'
          runVcpkgInstall: true
        env:
          VCPKG_DEFAULT_TRIPLET: ${{ matrix.vcpkg_triplet }}
          VCPKG_INSTALLED_DIR: '${{ github.workspace }}/vcpkg_installed'
          VCPKG_BINARY_SOURCES: 'clear;x-gha,readwrite'

      - name: Setup MSVC
        if: matrix.os == 'windows-2022'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ matrix.qt_version }}
          cache: true

      - name: Set Environment Variables
        shell: bash
        run: |
          if [ "${{ matrix.os }}" = "ubuntu-22.04" ]; then
            echo "PROJ_ROOT=/usr" >> $GITHUB_ENV
          elif [ "${{ matrix.os }}" = "macos-14" ]; then
            echo "PROJ_ROOT=$(brew --prefix proj)" >> $GITHUB_ENV
          elif [ "${{ matrix.os }}" = "windows-2022" ]; then
            echo "PROJ_ROOT=${{ github.workspace }}/vcpkg_installed/${{ matrix.vcpkg_triplet }}" >> $GITHUB_ENV
          fi

      - name: Cache CMake FetchContent
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/cmake_deps
          key: cmake-deps-${{ matrix.os }}-${{ hashFiles('CMakeLists.txt', 'src/CMakeLists.txt') }}
          restore-keys: |
            cmake-deps-${{ matrix.os }}-

      - name: Configure and Build
        shell: bash
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DFETCHCONTENT_BASE_DIR="${{ github.workspace }}/cmake_deps" \
            -DQT_PATH="${{ env.QT_ROOT_DIR }}" \
            -DPROJ_ROOT="${{ env.PROJ_ROOT }}"
          cmake --build build --config RelWithDebInfo

      - name: Test
        shell: bash
        run: |
          cd build
          if [ "${{ matrix.os }}" = "ubuntu-22.04" ]; then
            xvfb-run --auto-servernum ctest -C RelWithDebInfo --output-on-failure
          else
            ctest -C RelWithDebInfo --output-on-failure
          fi

  release:
    name: Publish GitHub Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Download All Artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ github.ref_name }}
          files: release-assets/*
          generate_release_notes: true